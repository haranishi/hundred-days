# ローカル専用の運用メモ（非公開の戦略・内部情報を含むため公開しない）
CLAUDE.md

# 公開前チェックの個人NGワードリスト（個人情報そのものなので公開しない）
.precheck-ng.txt

# wranglerのローカルキャッシュ（CloudflareアカウントIDを含むため公開しない）
.wrangler/

# ビルド成果物
dist/

# SNS投稿準備のローカル出力（動画の絶対パス等を含む）
.social-output/

# 依存
node_modules/

# Playwrightのローカルテスト成果物
test-results/
playwright-report/
blob-report/

# OS
.DS_Store

# ログ・環境変数
*.log
.env
.env.*
.dev.vars

# 各Dayのデータ変換の元ファイル置き場（入手先は各アプリのREADMEに明記）
day-*/tools/cache/

# デモ動画に音を重ねるための中間ファイル（record-demo が書き、render-demo-audio が読む）
day-*/.demo-sound.json

# プロモ動画ツールの生成物（BGMのWAV・プレビュー・本番mp4・1コマ目）。ソースだけを追跡する
day-*/tools/promo/promo-audio.wav
# 聴き比べ用に書き出すA案・B案も生成物（Day 039〜）
day-*/tools/promo/promo-audio-?.wav
day-*/tools/promo/promo.mp4
day-*/tools/promo/preview-contact.jpg
day-*/tools/promo/promo-first-frame.png

# 用途違いで不採用にした先行試作の退避先（Day 029 の家の中を測る試作）。追跡しない
.drafts/
