# Day別コード整理の検証記録

## 変更

- 全43個のDayフォルダーを `apps/` 配下からリポジトリ直下へ移動。
- READMEにDay・アプリ名・コード・公開ページの対応表を追加。Day 019・020は外部プロジェクトの制作記録と明記。
- ビルド、単体テスト、ブラウザテスト、API、制作補助ツールの参照パスを更新。
- `npm run index:sync` で一覧を更新できるようにし、ビルド時に更新漏れを検出。
- アプリ機能、依存関係、公開URL、素材、ライセンスは変更していない。

## ローカル検証

- 移動対象の追跡済み1,685ファイルを照合し、欠落なし。1,607ファイルはバイト単位で一致し、78ファイルは参照パスのみ更新。
- ビルド済みの全43Day、配信ファイル1,042個について移動前後のSHA-256が一致。制作メタデータ、文書、テスト、補助ツールは配信ファイルの比較対象外。
- `npm run build`: 成功。43Dayの一覧・共有ファイルの整合性を確認。
- `npm run test:unit`: 1,557件成功。
- Playwrightのテスト検出: 38ファイル、861件。
- 専用ポートでのブラウザ検証: 193件成功。対象は共有シェア機能、外部公開Day、セキュリティヘッダー、Day 035のAPI連携。
- 初回のブラウザ検証は既存ポート上の別アプリを参照して失敗したため、既存サーバーに手を加えず、専用ポートと `CI=1` で再実行した。
- `npm run precheck`: 漏洩パターン検出なし。既存の素材・文書に関する警告は、移動前との一致および既存の公開窓口・拒否テスト用URLであることを確認。
- `git diff --cached --check`: 成功。
- READMEの公開URL43件はすべてHTTP 200。READMEとAPI案内の相対リンク63件もリンク切れなし。
- 追加の参照先監査で、Day 009の撮影ツールのリポジトリ階層指定を修正。専用ポートでのサーバー自動起動と8パターンの撮影が成功。
- 既存の相対Markdownリンク・コードのimport参照530件を移動前後で照合し、新たな参照切れなし。

全件のブラウザ検証はPRのCIで実施する。CIの結果はPRのChecksを参照。
