# Day043 ローカル実装・検証記録

2026-09-19。対象は `day-043-moveout-consult/`。公開・commit・pushは未実施。

## 結果

- ビルド成功。Day043の画面・OGP・canonical・共通共有を既存の一覧へ収録。
- 全Dayの単体テスト：1,557件成功。Day043の43件は最終変更後も再実行して成功。
- Chromiumの対象ブラウザテスト：31件成功（Day043操作22・共有4・全体セキュリティ設定5）。
- `git diff --check` 成功。`npm run precheck` は漏洩パターン検出なし。
- precheckの494件の警告は画像・動画などの目視対象。今回の画面・動画は別途確認した。

## 実画面・体験の証拠

- `tools/verify-view.mjs` で390・768・1440pxを実測。横はみ出しなし、全入力にラベルあり。
- 証拠画像：アプリ内 `tools/cache/verification/{390,768,1440}-{start,memo}.png`。
- 負数を入力すると説明と該当欄へのフォーカスが出る。全角数字は受理。
- 未回答のままの完了、戻る、0円、部分合計、保存復元、保存・削除拒否、コピー失敗を確認。
- 任意保存は初期オフ。アプリ紹介のURL・本文に入力が含まれないことを確認。
- キーボードでの選択・手順移動・ダイアログの初期フォーカスとEscapeを確認。
- 入力中の外部リクエストなし。実画面の一例でDOM準備12ms、load35ms（ローカル測定）。
- 標準入力例のPDFはA4・1ページ。最大入力は2ページで、末尾の参考情報まで欠落なし。
- PDFとエラー・消去ダイアログの画像も同じ検証フォルダに保存。

## 独立レビューと修正

読み取り専用の独立レビューで、P0/P1はなし、P2が2件見つかった。

1. ストレージ削除失敗時に画面の消去まで阻止していた。
   画面は消し、下書きの残存・次回復元の可能性を警告する形へ変更した。
2. スマホの相談メモ本文が11pxと小さかった。
   画面の本文を14px、資料を13pxにし、印刷の9ptとは分けた。

再レビューで2件の解消を実操作・画像で確認済み。
コードを見ない別の評価者が実画面と操作を独立評価し、UIは19/20、体験は20/20だった。
UIはタイポグラフィが1点、ほか9項目は各2点。体験の10項目は各2点。未確認を点数で埋めていない。
390・768・1440px、コピー・テキスト保存・戻る・不正金額・保存復元・リセット・キーボードを確認した。
証拠は同じ検証フォルダの `independent-*.png`。
軽微な改善候補として、補足や出力ボタンの文字拡大、資料名同士の区切り文字の変更が残る。

## 紹介用の素材

- 一覧画像：`screenshot.webp`、1200×750。
- 一覧デモ：`demo.mp4`、15.28秒、720×1280。
- プロモ：`tools/promo/promo.mp4`、30.00秒、1080×1920、AAC 48kHz。
- プロモの音量：-17.10 LUFS、true peak -2.69 dBTP。既存の自作合成器を使用。
- 両動画の最初の0.1秒に画面差分あり。字幕と各場面はコマ一覧で目視確認。
- 使った金額は架空の例。メディアのメタデータに入力者の個人情報はなし。
- 完成素材は指定のDrive同期フォルダへ保存し、プロモのコピーはSHA-256一致を確認。
- ObsidianのDay043作業ログに、コードと完成素材の置き場所を記録した。

## 設計・文書

UX5階層とUI/UX Pro Maxを用い、3段階・未回答許容・入力とメモの併置へ反映した。
日本語文書はクイック工程で確認。READMEのlint指摘は0件。
要件と作業ログの文長リズムの警告は、短い事実記録の一貫性を優先して変更しなかった。

## 未確認・制約

- Safari系：Playwrightが要求するWebKit実体がなく起動できず、検証未実施。
- iPhone実機、OSの実共有シート、スクリーンリーダー、実プリンターは未確認。
- Obsidianアプリが未起動のため、読み取り用CLIは接続できなかった。ノートはファイル保存済み。
- 全Dayの全E2Eは実行していない。今回のアプリと共有・セキュリティ設定を対象にした。
- 公開先のヘッダー適用・実URL到達・X投稿キットは公開後に確認する。公開許可とは扱わない。

## 追加依頼後の公開前確認

本人からPR作成・マージとX投稿文の準備を依頼され、公開工程を契約に追加した。
precheck、build、全単体1,557件、対象E2E31件を再実行して成功した。
画面と紹介動画を再確認し、動画のメタデータに個人情報がないことも確認した。
利用条件・法的判断との線引き・入力の扱いは、アプリ内 `PUBLICATION_CHECK.md` にまとめた。
X本文は248／280換算。URLと動画を含むキットの検証は本番公開後に行う。
既存CIはPRで全E2Eを実行し、mainへのマージ後に再検証・自動公開する。
Xの実投稿はこの依頼には含まれない。
