# Day046 修正1周目

## 変更

- 光の尾を16分割の円錐に変更。X/Zの半径を等しくし、回転前のY軸だけを伸長。先端を猫の後ろに固定し、光速付近ほど長くする。
- カメラを猫へ向け、画角49度を縦横で共通にする。顔をカメラ側へ振り向かせる。
- 光速付近の猫の非表示・白い楕円への置換を廃止。猫の暖色発光を最大0.16に制限し、周囲に最大不透明度0.14の薄い輪を追加。
- snapshotに猫だけのBox3の8頂点を投影したcatScreenを追加。光の尾と輪は含めない。範囲外検出のため投影値をクランプしない。
- 押下説明を全幅の下端帯へ変更し、押下中は不透明度0.3にする。結果背景を不透明化。「この記録を共有」の見出しを追加。固定の「速度の実験 / 01」を削除。
- E2Eに3画面サイズ×指定4速度と6世界を網羅する速度点の構図チェックを追加。E2Eは実行していない。

## 検証

- Day046単体：54件 pass / 0件 fail。
- 全体単体：1,758件 pass / 0件 fail / 0件 skipped。
- 初回追加テストでは0.16と0.15999999999999998の厳密比較で1件失敗。正規化レベル1以上では上限値を直接返すよう修正し、上記の最終結果を確認。
- npm run build：成功。46 Dayの生成完了。
- npm run precheck：成功。漏洩パターン検出なし、既存を含む要確認警告503件。警告全件の目視完了を意味しない。
- 構文チェック：app3d.js、scene.js、E2E仕様は成功。
- 新たな外部素材・ライブラリは追加していない。
- gitコマンドは直接実行していない。指定されたprecheck内部の既存git読み取りのみ。

## ブラウザ測定の未完了

実ブラウザ撮影・測定は未完了。ローカルサーバーがlisten EPERM、ChromiumがMachPortRendezvousServerのPermission deniedで起動できなかった。権限昇格が許可されない環境のため続行不可。実測値や目視合格を記載していない。

代替として、最終scene.jsのWebGLRendererだけをスタブに差し替え、同じThree.jsの猫生成・アニメーション・Box3投影をNode上で実行。canvas 1080×450、362×540、362×374で各2,424姿勢（101速度×24時点、口の開閉含む）を検査し、指定構図条件の違反0件。これはブラウザ実測ではなく、CSSの実レイアウトや描画品質の検証でもない。

以下は1080×450を仮定した投影計算の参考値（実ブラウザ測定の代用とはしない）。

| 速度 km/h | x | y | w | h |
|---|---|---|---|---|
| 0 | 0.503790 | 0.486260 | 0.131119 | 0.267723 |
| 1,000 | 0.503362 | 0.493087 | 0.155253 | 0.267635 |
| 100,000 | 0.503208 | 0.497373 | 0.171912 | 0.268325 |
| 1,079,144,923.7151（99.99% c） | 0.503528 | 0.498140 | 0.206714 | 0.284972 |

残件：実ブラウザで6世界を確認し、4速度のsnapshot実測値を取得すること。耳・ひげ・猫の色・光輪・尾の見え方、帯の押下時変化、結果の共有見出しを含む収まりは実画面未確認。
